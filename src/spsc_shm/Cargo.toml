[package]
name = "spsc_shm"
version = "0.1.0"
edition = "2024"
description = "Zero-copy SPSC ring buffer over shared memory (memfd + eventfd)"
license = "Apache-2.0"

# Standalone crate — not part of any workspace.
[workspace]

[dependencies]
libc = "0.2"
