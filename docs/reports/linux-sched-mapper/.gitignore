# Build products. The source is the artifact; the PDF is regenerated by
# `just build` (see the sibling rt-mixed-criticality report, same convention).
report.pdf
# Review renders produced by `typst compile report.typ page-{n}.png`.
page-*.png
