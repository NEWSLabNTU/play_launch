name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build-and-test:
    runs-on: ubuntu-22.04
    container:
      image: ghcr.io/newslabntu/play-launch-builder:humble
    defaults:
      run:
        shell: bash

    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        submodules: recursive

    - name: Build workspace
      run: |
        source /opt/ros/humble/setup.bash
        just build

    - name: Run tests
      run: |
        source /opt/ros/humble/setup.bash
        source install/setup.bash
        which dump_launch || echo "Warning: dump_launch not found in PATH"
        just test

    - name: Run checks
      run: just check
